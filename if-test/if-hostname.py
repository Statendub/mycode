#!/usr/bin/env python3

#hostname = 'MTG':
#if hostname == 'MTG':
#	print('the hostname was found to be mtg')

#above is template for manipulating search functions

#code below will be customizations of the if function 

hostname =input('what is your name?') # allows for the hostname to be declared by input

if hostname.upper() =='MTG':    # accepts input from user
	print('the hostname was found to be mtg')
	print('hostname matches expected config')

print('exiting the script')	
